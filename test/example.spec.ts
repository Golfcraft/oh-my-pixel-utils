import { Entity } from 'decentraland-ecs'
import { createPixelClickHandler } from '../src/createPixelClickHandler'

describe('Oh my pixel test', () => {
  //(globalThis as any).Component = Component

  /*it('Should return constant', () => {
    expect(constant).toBe('constant')
  })

  it('Should return component', () => {
    const component = new ExampleComponent(true)
    expect(component.testing).toBe(true)
  })*/

  it('No test', () => {

    /*const callback = () => {}

    const plane = new Entity()
    const {Dispose, HideCursor, AddCursor} = createPixelClickHandler(plane, [10, 10], callback)
    expect(Dispose).toBe(Function);
    expect(HideCursor).toBe(Function);
    expect(AddCursor).toBe(Function);*/
  })
})